import Foundation // Подключаем стандартные инструменты Swift для работы с данными

// Создаём класс SkillsViewModel
// class — это как описание объекта, который может хранить данные и выполнять действия над ними
class SkillsViewModel: ObservableObject {
    // ObservableObject — позволяет View (экрану) автоматически обновляться, когда что-то изменяется в этом классе.
    // @Published — говорит SwiftUI следить за изменениями в массиве skills и обновлять экран, если он меняется
    @Published var skills: [Skill] = [
        Skill(id: UUID(), name: "Stick-to-it'ness"), // Тестовый навык "Упорство"
        Skill(id: UUID(), name: "Planning") // Тестовый навык "Планирование"
    ]

    // Пример из реальной жизни:
    // SkillsViewModel — это как табличка на стене, где каждый день ты записываешь новые привычки.
    // Если добавляешь новую привычку — табличка обновляется, и все видят изменения.
}
